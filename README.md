# ENCOR_LAB_HSRP
HSRP: Hot Standby Router Protocol is a cisco proprietary protocol used for redundant gateway allow the user to communicate even if the primary gateway is down. HSRP uses a virtual ip address as a default gateway instead of any physical ip address. There could be a active router standby router and listening state router based on priority higher wins
